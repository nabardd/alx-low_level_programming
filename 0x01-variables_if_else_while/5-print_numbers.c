#include <stdio.h>

/**
 * main - function to print digits from 0 to 9
 *
 * Return: 0, prints numbers 0 - 9
*/

int main(void)
{
	int n = 48;

	while (n <= 57)
	{
		putchar(n);
		n++;
	}

	putchar('\n');

	return (0);
}
